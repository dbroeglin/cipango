package org.cipango.media.api;

/**
 * A Managed object is managed by {@link MediaFactory}.
 * 
 * An object that will be created by {@link MediaFactory} and which will
 * be referenced by {@link MediaFactory}. This interface contains no method
 * as it just defines a property of a class.
 * 
 * @author yohann
 */
interface Managed
{

}
